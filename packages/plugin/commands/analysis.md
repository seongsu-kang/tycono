---
description: "Show wave analysis — role-by-role status, tokens, cost breakdown + orphan session detection. WHEN: user asks about cost, token usage, who is working, session analysis, or wants to check for zombie processes."
---

Run the analysis script to show detailed wave breakdown:

```bash
"${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/plugins/cache/tycono/tycono/0.1.0}/scripts/analysis.sh"
```

Show the output to the user as-is.
