---
description: "Generate a wave report — directive, dispatch tree, role results, cost breakdown. WHEN: user asks for a wave summary, report, recap, or wants to save wave results."
---

Run the report script to generate a wave summary:

```bash
"${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/plugins/cache/tycono/tycono/0.1.0}/scripts/report.sh"
```

Show the output to the user as-is. This is a markdown-formatted report.
