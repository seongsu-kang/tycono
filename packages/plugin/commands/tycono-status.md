---
description: "Check Tycono wave progress"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/status.sh *)", "Bash(${CLAUDE_PLUGIN_ROOT}/scripts/status.sh)", "Bash(curl *)"]
---

# Tycono Status

Run the following Bash command to check wave status:

```
"${CLAUDE_PLUGIN_ROOT}/scripts/status.sh"
```

IMPORTANT: You MUST execute the above command using the Bash tool. Do NOT skip this step.

Report the status to the user.
