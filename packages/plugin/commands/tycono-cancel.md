---
description: "Cancel active Tycono wave"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel.sh *)", "Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel.sh)", "Bash(curl *)"]
---

# Tycono Cancel

Run the following Bash command to cancel the active wave:

```
"${CLAUDE_PLUGIN_ROOT}/scripts/cancel.sh"
```

IMPORTANT: You MUST execute the above command using the Bash tool. Do NOT skip this step.

Report the cancellation result to the user.
